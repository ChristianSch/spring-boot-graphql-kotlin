query bookDetails($id: ID) {
    bookById(id: $id) {
        id
        name
        pageCount
        published
        author {
            id
            firstName
            lastName
            socialMedia
        }
    }
}